require('core.lazy')

require('core.keymaps')

require('core.options')

-- require "lazy-bootstrap"

-- require "settings"
-- require "keybindings"

-- local lazy = require("lazy")
-- lazy.setup("plugins")
